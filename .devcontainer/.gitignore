# Per-project agent state — keep local, never commit.
dotfiles/.claude/
dotfiles/.gemini/
dotfiles/.codex/
dotfiles/.config/gh/
# .ssh directory itself is tracked (contains only .gitignore); contents are not.
dotfiles/.ssh/*
!dotfiles/.ssh/.gitignore
dotfiles/.zsh_history
